<!-- intent-skills:start -->
# Skill mappings - when working in these areas, load the linked skill file into context.
skills:
  - task: "Building API endpoints and backend logic with createServerFn"
    # To load this skill, run: pnpm exec intent list | grep server-functions
  - task: "Routing configuration, creating routes, and client-side navigation"
    # To load this skill, run: pnpm exec intent list | grep router-core
  - task: "Route data fetching, preloading, and handling cache invalidations"
    # To load this skill, run: pnpm exec intent list | grep data-loading
  - task: "Managing server-side rendering (SSR), Suspense, and document shells"
    # To load this skill, run: pnpm exec intent list | grep ssr
<!-- intent-skills:end -->
