<!-- intent-skills:start -->
# Skill mappings - when working in these areas, load the linked skill file into context.
skills:
  - task: "configuring bidirectional events between app and devtools"
    load: "apps/public-tanstack-start/node_modules/@tanstack/devtools-event-client/skills/devtools-bidirectional/SKILL.md"

  - task: "creating a devtools event client and connection lifecycle"
    load: "apps/public-tanstack-start/node_modules/@tanstack/devtools-event-client/skills/devtools-event-client/SKILL.md"

  - task: "adding strategic event emissions for devtools instrumentation"
    load: "apps/public-tanstack-start/node_modules/@tanstack/devtools-event-client/skills/devtools-instrumentation/SKILL.md"

  - task: "configuring devtools vite plugin and server event bus"
    load: "apps/public-tanstack-start/node_modules/@tanstack/devtools-vite/skills/devtools-vite-plugin/SKILL.md"

  - task: "working with React bindings for TanStack Start (createStart, StartClient)"
    load: "apps/public-tanstack-start/node_modules/@tanstack/react-start/skills/react-start/SKILL.md"

  - task: "implementing React Server Components in TanStack Start"
    load: "apps/public-tanstack-start/node_modules/@tanstack/react-start/skills/react-start/server-components/SKILL.md"

  - task: "configuring TanStack Router Vite plugin and automatic code splitting"
    load: "apps/public-tanstack-start/node_modules/@tanstack/router-plugin/skills/router-plugin/SKILL.md"

  - task: "working with TanStack Router core routing concepts and file conventions"
    # To load this skill, run: pnpm exec intent list | grep router-core

  - task: "implementing route protection, layout routes, and authentication guards"
    # To load this skill, run: pnpm exec intent list | grep auth-and-guards

  - task: "configuring automatic code splitting and lazy route components"
    # To load this skill, run: pnpm exec intent list | grep code-splitting

  - task: "fetching data for routes, configuring loaders, and caching"
    # To load this skill, run: pnpm exec intent list | grep data-loading

  - task: "implementing links, navigation, and scroll restoration"
    # To load this skill, run: pnpm exec intent list | grep navigation

  - task: "handling not found routes and catch boundaries"
    # To load this skill, run: pnpm exec intent list | grep not-found-and-errors

  - task: "defining dynamic path segments and path parameters"
    # To load this skill, run: pnpm exec intent list | grep path-params

  - task: "validating and managing URL search parameters"
    # To load this skill, run: pnpm exec intent list | grep search-params

  - task: "configuring server-side rendering and streaming in TanStack Router"
    # To load this skill, run: pnpm exec intent list | grep ssr

  - task: "working with strict type inference and type safety in routes"
    # To load this skill, run: pnpm exec intent list | grep type-safety

  - task: "working with TanStack Start core components and document shell"
    # To load this skill, run: pnpm exec intent list | grep start-core

  - task: "deploying TanStack Start applications and configuring SSR modes"
    # To load this skill, run: pnpm exec intent list | grep deployment

  - task: "managing isomorphic code and environment variable safety"
    # To load this skill, run: pnpm exec intent list | grep execution-model

  - task: "creating context middleware for server and client functions"
    # To load this skill, run: pnpm exec intent list | grep middleware

  - task: "creating server functions and backend endpoints"
    # To load this skill, run: pnpm exec intent list | grep server-functions

  - task: "building server-side API routes and HTTP handlers"
    # To load this skill, run: pnpm exec intent list | grep server-routes

  - task: "working with server-side request/response utilities"
    # To load this skill, run: pnpm exec intent list | grep start-server-core

  - task: "building programmatic virtual file route trees"
    # To load this skill, run: pnpm exec intent list | grep virtual-file-routes
<!-- intent-skills:end -->
