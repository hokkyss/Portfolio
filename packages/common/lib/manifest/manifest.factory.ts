import { Manifest } from './manifest.interface';

/**
 * Typesafe-ly define manifest.json configuration.
 * @param manifest - The manifest configuration.
 * @returns The manifest configuration.
 */
export function defineManifest(manifest: Manifest) {
  return manifest;
}
