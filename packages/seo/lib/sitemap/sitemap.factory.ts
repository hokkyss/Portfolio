import { Sitemap } from './sitemap.interface';

/**
 * Typesafe-ly define sitemap.xml configuration.
 * @param sitemap - The sitemap.xml configuration.
 * @returns The sitemap.xml configuration object.
 */
export function defineSitemap(sitemap: Sitemap) {
  return sitemap;
}
