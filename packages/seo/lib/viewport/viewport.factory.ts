import { Viewport } from './viewport.interface';

/**
 * Typesafe-ly define viewport configuration.
 * @param viewport - The viewport configuration.
 * @returns The viewport configuration object.
 */
export function defineViewport(viewport: Viewport) {
  return viewport;
}
