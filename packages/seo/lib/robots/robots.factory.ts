import { Robots } from './robots.interface';

/**
 * Typesafe-ly define robots.txt configuration.
 * @param robots - The robots.txt configuration.
 * @returns The robots.txt configuration.
 */
export function defineRobots(robots: Robots) {
  return robots;
}
