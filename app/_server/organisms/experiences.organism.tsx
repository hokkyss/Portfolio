import { cn, tw } from '~/_common/utils/classname.util';

export default async function Experiences() {
  return (
    <section className={cn(tw`h-screen bg-gradient-to-b from-blue-600/50 to-amber-950/60 dark:from-blue-950/80`)}>
      Yo
    </section>
  );
}
